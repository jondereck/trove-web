# Notifications reminder organization

**Date:** 2026-09-01  
**Status:** Approved  
**Scope:** Mobile (`trove`) + Web (`trove-web`)

## Goal

Keep Trove theme tokens. Reorganize the Reminders tab:

- **UPCOMING** with buckets: Today · Tomorrow · This week · Later (hide empty)
- **COMPLETED** collapsible (default collapsed) with count badge
- Shared pure helper for bucketing by `fireAt`

Inbox tab unchanged in structure.
